{
  "name": "missingmigrants",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --no-minify",
    "build": "parcel build --public-url . index.html --no-minify",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "cssnano": "^5.1.11",
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.3.1",
    "postcss": "^8.4.14"
  },
  "dependencies": {
    "ol": "^6.14.1"
  }
}
